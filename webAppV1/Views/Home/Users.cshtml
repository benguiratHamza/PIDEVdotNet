@model IEnumerable<webAppV1.Models.User>
@{
    ViewBag.Title = "Users";
}

<h2>Users</h2>
@if(Model != null) { 
foreach (var item in Model)
{
    @Html.DisplayFor(modelItem => item.Name)
    <br />
}
}
